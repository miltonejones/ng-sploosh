import { HtmlTooltip } from './html-tooltip';

describe('HtmlTooltip', () => {
  it('should create an instance', () => {
    const directive = new HtmlTooltip();
    expect(directive).toBeTruthy();
  });
});
