import { ImageSource } from './image-source';

describe('ImageSource', () => {
  it('should create an instance', () => {
    const directive = new ImageSource();
    expect(directive).toBeTruthy();
  });
});
